name: Step 1 - Manual Trigger Test

on:
  workflow_dispatch:

jobs:
  echo-test:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: echo "✅ Workflow manual ejecutado correctamente"
